node_modules/
dist/
coverage/
*.log

# CI sets PLAYWRIGHT_BROWSERS_PATH here; never present in a normal local checkout
.pw-browsers/

# local environment / secrets
.env
.env.*

# scan output (never committed — may contain third-party site data)
/reports/
*.bfsg-report.*

# private working files (site lists, client data — see docs/SCANNING-ETHICS.md)
/private/
sites.private.*
